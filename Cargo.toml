[package]
name = "graphql-compare"
version = "0.1.0"
edition = "2024"

[dependencies]
apollo-compiler = "1.29"
apollo-federation = { version = "2.5", features = ["correctness"] }
clap = { version = "4.5", features = ["derive"] }

